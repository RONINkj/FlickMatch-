# FlickMatch

Movie & Series recommender with:
- FastAPI backend (in `FlickMatch/`)
- React + Vite frontend (in `flickmatch-frontend/`)

## Structure

- `FlickMatch/` – FastAPI backend, recommendation logic, TMDB integration
- `flickmatch-frontend/` – React UI, poster wall, search, modals

## Backend (FlickMatch)

_Copy / adapt the useful setup and run steps from the backend README here._

## Frontend (flickmatch-frontend)

_Copy / adapt the useful setup and run steps from the frontend README here._
